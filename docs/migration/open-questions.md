# Open Questions Register

Every decision in [`PLAN.md`](./PLAN.md) that waits on an answer from [`qa.md`](../../qa.md). The questions themselves live in `qa.md` and are referenced by number here, never restated.

**Numbering:** rows cite `qa.md`, the current draft. The earlier [`qa1-1.md`](../../qa1-1.md) numbers its first 23 questions identically, then diverges, because `qa.md` inserts seventeen questions — the Monthly Billing Run and Payment Processing sections — at 24. From there on, `qa.md` = `qa1-1.md` + 17. The context package's Part C uses a third numbering (its shift-start PIN is Q87 where `qa.md` has Q83), and the Round 2 document ([`BlueBuoy_Round2_Questions.docx.md`](./BlueBuoy_Round2_Questions.docx.md)) a fourth (Round 2 Q1–Q20; its answers are transcribed into `qa.md`, which is what rows cite). Citations to either name the document explicitly. A bare question number with no document is meaningless — never write one.

**Reconciliation sweep, 2026-09-07:** every `open` and `assumed` row was checked against [`BlueBuoy_Complete_Context_Package.md`](./BlueBuoy_Complete_Context_Package.md), whose Part A folds in staff and ownership answers. Rows the package answers are flipped to `answered`, citing the package section and its confidence label. Three assumptions were **contradicted** by Confirmed findings (authorization tracking, payment documentation, refund basis) — the affected rows say so, and the schema redesign is tracked on the wayfinder map (issue #1, ticket #4). Four newly-surfaced questions gained rows, marked **(new)**.

**Reconciliation sweep 2, 2026-09-10:** the second staff round came back answered out of band (Round 2 Q1–Q20, now `qa.md` Q36, Q59–Q61, Q99–Q101, and new Q103–Q108) together with a revised context package. Six rows flipped to `answered` (group structure, conversion ratios, the live file, the first-responder discount, processing volume, and — as a new row — the reports staff run); eligibility stays `assumed` with the group bars now exact; three new rows were added (Adult class packs, the `BlueBuoy_FM_2024` archive, substitute finding); and two design consequences went to the wayfinder map as tickets: the make-up credit ledger becomes one denominated system, and the Adult class needs a second billing model.

**Data profile, 2026-09-16:** the live export of 2026-09-13/14 was profiled ([report](../discovery/data-profile-2026-09-14.md)). No row flips; four rows below gain a *Profile:* note with the measured fact, and the archive row gains the count of students the live file no longer holds.

**Reconciliation sweep 3, 2026-09-16:** the export session of 13–14 September produced an addendum ([findings](./BlueBuoy_Export_Session_Findings.md)) carrying a third staff round and ownership decisions, plus a v3 package (§2.4 make-up offers rewritten around minimising held time; §3.9 closures gain bulk issuance and a once-per-family proration waiver). One row flipped to `answered` (the caps convention becomes a flag), one `open` row to `assumed` (the offer window), six rows gained findings, six new rows were added (marked **(new)**), and `qa.md` gained Q111–Q115. The legacy card scrub is ruled out of the map's scope as legacy operations.

**The rule:** build on an assumption freely; confirm it before it prices anything, enforces anything, or migrates anything irreversibly. An assumption reaching the money path unconfirmed is the failure this register exists to prevent.

**States:** `open` (no answer, no working assumption) · `assumed` (working assumption recorded below, safe to build on) · `answered` (confirmed; update the row and the affected section of `PLAN.md`).

## Blocks Foundations — the schema cannot settle without these

| Decision | qa.md | Working assumption · answer | State |
| --- | --- | --- | --- |
| Household or student as the billing account | Q49 | Household owes; student carries enrollment and payer. Both stay addressable. Package §7 recommends family-level billing with student line items but leaves the call to ownership (Part D). | open |
| Sibling discount: still policy, and is the 4th-slot cap deliberate | Q1 | Mechanics Confirmed in the DDR: full rate, then −1/−2/−3 discount steps, capped at 3× from the 4th slot. Whether the cap is *intended policy* still needs ownership (package 3.5, Part D). Encoded as ordered discount steps with the cap as data. | assumed |
| Class eligibility: exact age and level bars per type | Q60–Q63 | Age AND level. **Group bars Confirmed (Round 2 Q5; package 3.1):** Parent & Me ages 0–3 with no level requirement; Group / Stroke Prep age 7+ and level 9–12; Stroke Tech age 10+ and level 10–12; Adult age 16+; all four reported to cap at 6, though the DDR's slot calculation gives Adult **5** seats (Q110). The Deck Manager screenshot returned with Round 2 shows one Group class banded `AGE 7+ lv 8/9/10` beside one banded `lv 9-10`, so bands are set per class and at least one admits level 8 — hold the rule per slot, not only per type. Private and Semi-Private requirements (Q61), whether age or level ever substitutes (Q62), and exception practice (Q63) still need staff. | assumed |
| Whether eligibility exceptions need an audit trail | Q63 | Override carries a reason and an author. Package 3.1 concurs: warn, allow override with reason. Findings §2.4/§3.2: the legacy `Audits` table records only schedule deletions (5,958 rows), so no override trail exists today; ownership wants a short set of override actions each carrying who and a one-line reason — modelled as `audit_event`. | assumed |
| Payer mix within one household | Q11 | **Confirmed (package 2.10):** mixed payers within one family are common; payer assignment is per student. New requirement: staff deliberately place the institutionally-funded child in the undiscounted first tier so the out-of-pocket sibling gets the discount — tier assignment must stay manually controllable. | answered |
| Prepay tiers actually in use, and what earns each | Q14 | **Confirmed (package 3.7):** duration-based — 4-month = 5%, 8-month = 10%. The full 0–100% ladder exists in FileMaker; which other tiers are genuinely offered still needs enumeration before seeding. | answered |
| Whether make-up credits expire | Q47 | None. Ownership decision still open; balances of 24–28 unused credits appear normal (package 3.10, Part D), and with the conversion ratios now confirmed (Q100) the liability is larger than the raw count — 24 semi-private credits are also 12 private lessons. A real liability call, not housekeeping. Designed as a rule row (`makeup_expiry_months`, null = never) stamped onto each credit at issue, so the answer is a row ([ADR-0003](../adr/0003-denominated-makeup-credit-ledger.md)). | open |
| Which note abbreviations mean what (`AUG PO`, `WORK`, `MU`, `OUT`) | Q69 | Partial decode (package 3.3, 3.8): `$ OCT`-style notes = a billing adjustment due that month (Confirmed); `WORK`, `AUG PO` = hold reasons stored as text. Full glossary still needed; import preserves raw note text until decoded. | open |
| Which colour highlights and typing conventions carry meaning | Q67, Q68 | **Confirmed (package 3.3):** student first name in ALL CAPS = special needs (verified against `flag_Special_Needs`); parent first name in ALL CAPS = difficult account; bold at top of account = permanent info incl. UCI number and coordinator contacts; age highlight = under-4 swim-diaper requirement; blue/pink = gender; red note = needs attention; `$ OCT` = scheduled adjustment. *Profile:* 49 stored student first names are ALL CAPS without `flag_Special_Needs` (13 with it); 138 primary and 113 secondary contact first names are ALL CAPS — the decode list is small. **Round 3 (findings §2.1):** student caps are formula-driven from the flag and nobody types them; last-name caps are noise to title-case; parent caps are hand-typed and mean "handle with care". | answered |
| Whether the ALL CAPS convention becomes a typed flag | Q71 | **Confirmed (findings §2.1, ownership):** `support_need` drives the student display; `account_handling` is an office-only flag with a short reason note, and office screens render the guardian's name in caps when it is set so the glanceable convention survives — never instructor- or parent-facing. Import decodes the 138 primary and 113 secondary caps names into it. | answered |
| How price changes actually propagate, given that pre-created future billing months keep the old rate while newly created ones take the new | `qa1-1.md` Q3, Q16 | **Confirmed (DDR + package 3.6):** rates are stamped by auto-enter at row creation; next year's rows are pre-generated in bulk, so pre-created months keep old rates while newly created rows take new ones. Prepay locks are maintained entirely by hand — twelve monthly negative adjustments per prepaid family per year. Import treats future billing months as intentions, never invoices (`PLAN.md` §5, Billing phase). | answered |
| Make-up credit conversion ratios between lesson types | Q100 | **Confirmed (Round 2 Q7–Q11):** 2 semi-private = 1 private; 1 private = 2 semi-private; 2 group = 1 semi-private; 4 group = 1 private; 1 private = 4 group — Group, Stroke Tech and Parent & Me are one "group" denomination. Every direction is permitted; staff choose at redemption, nothing converts automatically; a student who moves to group keeps private credits as private; credits are moved between siblings today as a manual note. The six legacy counters are units of one currency — settled by [ADR-0003](../adr/0003-denominated-makeup-credit-ledger.md): three denominations, whole-lesson units, conversion inside redemption, reserve-then-consume, replayed import. | answered |
| Which FileMaker file is live: `BlueBuoy_FM` or `BlueBuoy_FM_2024` | Q99 | **Confirmed (Round 2 Q18):** `BlueBuoy_FM` is live and the analyzed schema is canonical. `BlueBuoy_FM_2024` is an archive: families with no enrollment for ten years are moved out because the live file slows, and they must stay reachable in case a family returns. | answered |
| **(new)** The `BlueBuoy_FM_2024` archive: what the import does with it | Q99 | The archive's schema has not been analyzed and its row counts are unknown. Whether archived families import as archived households (restorable, never deleted), stay only in the frozen read-only FileMaker archive, or both, is a map ticket; performance-driven archiving itself ends at cutover. *Profile:* 5,654 student ids referenced by 214,642 billing-month rows (1,444 with a payment) and 12,478 billing-year rows are absent from the live file — the archive is where the import must look for them, and the archive export is still owed. | open |

## Blocks Scheduling & search

| Decision | qa.md | Working assumption · answer | State |
| --- | --- | --- | --- |
| Which searches run daily, and which Saved Finds exist | Q55–Q58 | Still open for the ad-hoc set and the Saved Finds. Round 2 named, unprompted, the searches staff live in: the schedule board's absence lookup (`Search Out`), the waitlist open-requests report, the four `General_Reports_NEW` buttons (teacher notes, Deck Manager notes, trials, lesson attendance), the monthly prepays-ending search, and the monthly tuition-vs-Authorize.Net-batch check — all cross-referenced in the [parity enumeration](../discovery/search-parity-enumeration.md) Part 5. Parity cannot be asserted until batch 1 Section C returns the rest. | open |
| Waitlist matching: what staff actually do to place a child | Q52–Q54 | **Confirmed (package 2.4, 2.5):** staff curate every placement and the system never auto-books; stated availability is a ranking signal, not a filter (families are routinely more flexible than filed); offers go to one family at a time — a manual convention today (`8/27 offrd T` in a notes field) that the offer queue formalizes. | answered |
| Whether group lessons schedule or price differently | Q59 | **Confirmed (Round 2 Q1–Q5):** group classes run on a fixed annual timetable, so joining is a seat question; a class runs with one student or none; an entirely empty group slot may be lent for the day to SP / PR / make-ups / PM when a sub is needed; price does not vary with fill; staff deliberately place the group lesson in the first, undiscounted sibling tier. Adult is a fourth group type with its own billing model (Billing table below). | answered |
| Late-cancellation cutoff, and whether it becomes a rule | Q46 | Current practice Confirmed discretionary (package 3.10); whether to formalize a cutoff is an ownership call. System records the decision either way. | open |
| Master closure calendar, or ad hoc | Q45 | Closures entered as they arise; the calendar is the new capability. DDR supports it: `Holiday_Dates` holds ~6 rows — a working set, not a durable calendar (package 3.9, Inferred). *Profile:* six rows confirmed in the live file on 2026-09-14; the table has no layout and is still to be exported. | assumed |
| Offline need on the pool deck | Q85, Q86 | Attendance works offline and syncs; assume dead zones exist. Package 2.1 raises the bar: cache the **full day's schedule for all instructors** on every device — dead-battery iPad handoffs are routine. | assumed |
| Whether attendance records who marked it | Q84 | Recorded silently, no extra step for instructors. Package 1.2 concurs: attribution as a passive byproduct at most — confirm staff want it at all. | assumed |
| PIN at shift start acceptable | Q82, Q83 | **Confirmed (package 1.2):** PIN at shift start establishes identity; switching the displayed schedule and marking your own attendance from any device stays instant and password-free — device independence is a hard requirement. Bounded PIN-session window, auto re-lock on inactivity, remote revocation. | answered |
| **(new)** Make-up offer response window | Q102 | **Round 3 direction (package v3 §2.4, Confirmed with the billing/scheduling lead):** short — an hour or two, never a day, because every held slot blocks another family; one or two options per offer, not a shortlist; adjustable per offer. Working assumption: rule row `makeup_offer_window_hours` = 2. The default, whether cascade is automatic or approved, and whether a family is told when it loses the slot go to batch 2 (Q113). | assumed |
| **(new)** Held seat vs instructor time block | Q111 | **Confirmed (findings §3.1):** two mechanisms — a student-seat hold (semi-private slot blocked from a second student, review date defaulting to four weeks) and an instructor time block (regular break or ad hoc, day-level lifetime) — both excluded from availability everywhere, never one flag. Modelled as `seat_hold` and `instructor_block`; what legacy hold and break rows import as is the schedule-row ticket. | answered |
| **(new)** Enrollment offer pending payment: how long the slot holds | Q112 | **Confirmed (findings §2.2):** until 5 pm the following day; unpaid, the hold releases, the slot returns to availability and the payment request is cancelled. Rule row `enrollment_offer_deadline`. | answered |
| **(new)** Make-up offer cascade and loss notice | Q113 | Whether an expiring offer advances automatically or on approval; whether the family is told the slot was taken; whether a creditless request still queues. Carried as rule rows (`offer_cascade_auto` default false) until batch 2 answers. | open |
| **(new)** The legacy Christmas boundary on make-ups | Q109 | The FileMaker redemption script refuses a make-up on any lesson ending after December 25 ("cannot be used as a MU. End date is after Christmas") — an effective year-end expiry nobody has mentioned, found in the DDR. Carried as rule row `makeup_redeem_before`, seeded to match legacy, until staff say whether it is policy or a rollover artifact ([ADR-0003](../adr/0003-denominated-makeup-credit-ledger.md)). | assumed |
| **(new)** Which make-up balance is the truth at import | Q47, Q100 | **Decided ([ADR-0005](../adr/0005-makeup-balance-reconciliation-at-import.md)):** FileMaker's stored counters are stale (they match its own formula for 14–31% of active students) and cannot be separated from deliberate hand edits, so the ledger replays the rows and a `reconciliation` event per student per denomination bridges to the stored counter; no balance changes at go-live; negatives become zero with a review; Unknown is dropped to the adult review list; the office reviews the active population, management corrects; the Scheduling & search gate is rewritten around it. Batch 2 carries only the review list's size and whether to accept the unreviewed remainder wholesale. | answered |

## Blocks Billing and Institutional payers

| Decision | qa.md | Working assumption · answer | State |
| --- | --- | --- | --- |
| How a charter school is billed, start to finish | Q4, Q5 | **Confirmed (package 2.10):** rates negotiated ~July per school, deliberately above the auto-pay rate; families obtain their own funding/purchase orders; Blue Buoy invoices at month end against the PO — some schools by emailed/paper invoice, others through the school's own portal; payment lands a month or more later. Today's entire AR trail is free-text notes (`JAN 11237 $368`, `CK 7499`). | answered |
| Whether institutional payers authorize lessons or dollars, with expiry | Q6, Q7 | **Confirmed (package 2.10) — corrects the recorded assumption:** neither. Families hold their own funding; Blue Buoy has no visibility into balances and nothing to track against a cap. The real needs are a month-end "no PO yet" prompt and receivables aging. The replacement model is decided: funding references, never balances ([ADR-0002](../adr/0002-no-authorization-balance-tracking.md)). | answered |
| Whether Regional Center billing differs from charter billing | Q9 | **Confirmed (package 2.10):** it differs — annual family-arranged contracts, rates kept ≈ the auto-monthly rate (charters deliberately higher), some agencies require invoices while at least one sends checks directly; students carry a UCI number plus coordinator contacts, today as bold notes. | answered |
| Documentation required to release payment | Q12 | **Confirmed (package 2.10) — corrects the recorded assumption:** none. No attendance sheets or service logs are required by any payer. | answered |
| Negotiated institutional rates | Q10 | **Confirmed (package 2.10):** per-school contract rates set annually ~July; Regional Center ≈ auto-monthly rate. Per-payer price agreements stand. | answered |
| The real month-end steps and where the time goes | Q24–Q26, Q34 | **Confirmed (package 2.8):** the five understood steps are right. 6–7 hours in a normal month, 10–12 across two days in summer (~100 h/yr). Worst step: posting each family's lesson type at the right tier. Card mismatches occur monthly; the office misses a mid-month change 1–2×/month, caught only at the next run. | answered |
| Mid-month lesson type change: what gets charged | Q31 | **Confirmed (package 2.8):** prorate at the tier the student currently occupies — tier position is preserved across the type change (Parent & Me 1st tier → Semi-Private 1st tier). | answered |
| Proration rule for partial months | Q24, Q27 | Per-lesson proration against scheduled lessons in the period. Package 2.8 confirms proration is occasional (end-of-summer peak) and should compute from scheduled lessons; the exact arithmetic is still unconfirmed. | assumed |
| Prepay rate lock duration | Q15 | **Confirmed (package 3.7, 3.8):** the lock holds until the prepaid months are consumed. On an enrollment hold it survives up to one year from the pause (months restored at the locked rate); past a year it converts to a dollar credit at current rates — and that dollar credit never expires. | answered |
| Refund basis when a prepaid family leaves early | Q18 | **Confirmed (package 3.7) — corrects the recorded assumption:** months actually taken are recalculated at the tier the family genuinely earned, and the difference refunded (an 8-month 10% prepay stopped at month 4 recomputes those months at the 4-month 5% tier). Not "refund unconsumed lessons at the rate paid". | answered |
| What else adjustments are used for | Q20 | **Confirmed (package 3.6, 2.7):** dominated by the manual prepay-lock back-outs (12 per prepaid family per year — retired by price agreements) and `$ OCT` referral resets; plus courtesy credits and corrections. The full payment/adjustment type list is enumerated in the DDR. | answered |
| Void versus refund window and who decides | Q33 | **Confirmed (package 2.8):** refunds run after the 1st but before that week's scheduled lesson — roughly one week; deliberately discretionary, sometimes a make-up is offered instead. Preserve the discretion. | answered |
| Whether the two-payments-per-month cap matters | Q22 | **Confirmed (DDR; package 2.7):** `Amount_Paid_1/2` is a FileMaker structural limit; the new transaction ledger removes it. | answered |
| Card-on-file failures and follow-up today | Q39, Q40 | Handled manually. Account Updater status still unknown (package Part D). | open |
| Processing stack confirmation | Q35–Q38 | **Confirmed (Round 2 Q17; package 2.9):** Affinity24 → Authorize.Net → Wells Fargo stays (rule 6). Card volume is **$156k–$207k a month** across 550–700 families/students; institutional check and direct-deposit income is now ≈15% of the total; 2026 gross ≈ $2.65M. Effective rate (Q37) and Account Updater status (Q39) still unknown. *Profile:* the export shows 820–1,150 payments a month over the last 24 months totalling $154k–$278k, of which 640–900 are `Auto CC` and 66–250 `Credit Card` — the card count runs above staff's 550–700. **Round 3 (findings §2.2):** every card is in Authorize.Net and monthly charges run from there; FileMaker's card copies are redundant, so the legacy scrub can start at once — legacy operations, outside the map. | answered |
| **(new)** Payment requests so office staff never handle a card | Q35 (Round 3) | **Confirmed direction (findings §2.2, ownership):** the office requests, billing sends a hosted payment link by SMS carrying the amount computed from the account, the family enters its own card, and the payment posts back automatically; today invoicing is enabled and one person sends. Which Authorize.Net product provides a per-household hosted link is a research ticket; the flow and adapter are a grilling ticket. | assumed |
| **(new)** Do-not-bill reasons and trade payments | Q51, Q115 | **Confirmed (findings §3.3–3.4):** about 20 households are "do not bill" for three reasons — trade, staff family, friends of staff — held as `billing_exemption` with a reason pick-list (Trade / Staff family / Courtesy / Other) and a note; a trade is a `payment` of method `trade` with a dollar value and note. How the accountant records trades is open (Q115). | answered |
| **(new)** Closure proration allowance scope | Q114 | Whole family or per child; whether a closure spikes competing make-up requests. Batch 2. | open |
| Whether December/January closure bills normally | Q41, Q42 | **Confirmed (package 3.9, 3.6):** full monthly amount, no credit — the two-week closure is absorbed into the flat annual tuition model (`Billing_Months` has no lesson-count field). | answered |
| Whether one-off closures always credit | Q43, Q44 | **Confirmed (package 3.9, 3.10, live records):** yes — a bulk closure action issues make-ups to everyone affected (Memorial Day and Presidents Day closures seen on real student records); the legacy "do not issue" guard covers the exceptions. **Package v3 §3.9:** once issued, a holiday credit is an ordinary credit; bulk issuance selects by weekday or by pool; a third path skips the credit and prorates tuition — a new student whose first lesson falls on a closure, or a retention gesture allowed once per family and tracked by the system (`closure_waiver`). Whether the allowance is per family or per child is open (Q114). | answered |
| First-responder ("military") discount: amount and stacking | Q101 | **Confirmed (Round 2 Q16):** 10% for military, police and fire families; stacks with both the sibling steps and the prepay discount; ID is requested to verify, and staff want a flag recording whether it was provided. Modelled as a household-level discount with verification fields. | answered |
| Adult class packs: how the Adult class is billed, and what happens on a no-show | Q61, Q110 | **Confirmed (Round 2 Q6):** Adult (`AD`) is a group class, age 16+, with **no monthly tuition** — adults buy a single class, a pack of 4, or a pack of 8, and a lesson is deducted only when the teacher's roll marks them present. **Design settled ([ADR-0004](../adr/0004-adult-class-packs.md)):** class packs are a billing mode on the enrollment, a purchase not a credit, consumed on attendance, never gating the roll. **Open with staff (Q110):** the no-show rule (today: seat lost, nothing deducted), pack expiry, whether household discounts reach a pack — all rule rows — plus two discrepancies the DDR raised: FileMaker prices Adult as a *monthly* tier (`Rate_Tier_AD`) and gives an Adult class **5** seats where staff said 6. | open |

## Shapes the build without blocking it

| Decision | qa.md | Note |
| --- | --- | --- |
| Past-due handling and whether lessons stop | Q50 | Drives dunning; a report suffices at launch. |
| What billing situations fall outside the system today | Q51 | Direct answer to what the replacement must absorb. **Round 3 (findings §3.3–3.4):** trades and the ~20 do-not-bill households, now rows (Billing table above). |
| Access model: who may price, credit, and see billing | Q80 | Roles seeded conservatively; widened on answer. Package §1 proposes four roles incl. the Deck Manager surfaced from `Instructor_Entry`. |
| Shared logins and offboarding | Q87, Q88, Q91 | Per-person accounts from day one regardless of answer. Package 1.3 confirms at least one shared login ("deck manager") exists today. |
| Backup and restore reality, tolerable downtime | Q93–Q95 | Sets the recovery objective for Cutover. Whether a FileMaker restore has ever been tested is still unknown (package Part D). |
| Bulk texting use and unmet wants | Q72–Q74 | Scopes the fmSMS replacement. Confirmed (package 2.11): fmSMS is a Databuzz shell over a third-party SMS gateway; inbound replies currently go nowhere and staff want them. **Gateway identified (Round 2 screenshot 8):** the fmSMS account is a Twilio account with a US sender number — the replacement talks to Twilio directly and keeps the number. |
| Reports staff run today, and the ones they want | Q103–Q106 | **Confirmed (Round 2 Q12–Q15):** daily — weekly enrollment by lesson type against the same week in prior years (hand-compiled into Excel today; ~1,100 enrollments a week), yesterday's trials to call back, and deck/instructor notes; weekly or monthly — waitlist open requests (staff want it automated away); monthly — prepays ending with enrollment continuing, and each enrolled student's tuition checked against the Authorize.Net batch for the 1st; annually — QuickBooks GL, P&L and balance sheet to the accountant; plus a per-agency Excel sheet tracking Regional Center payments. Painful to lose: the absence lookup, trial reports, notes, the per-student attendance sheet. Wanted: an auto-pay tuition summary, students absent 2+ weeks without notice, lesson-type changes on ongoing enrollments. Every one is a query over `PLAN.md` §2; the weekly comparison wants an enrollment snapshot job. |
| Substitute finding when a teacher calls out | Q107, Q108 | **Confirmed (Round 2 Q19–Q20):** the waitlist matching engine under time pressure, with three constraints the system must respect — required instructor gender, level-pairing tolerance, and sibling proximity — and an explicit on-call-teacher fallback. A lesson landing on a lifeguard shift happens only when someone is deliberately switched from guarding to subbing, so Humanity integration is not needed. |
| Instructor-authored progress notes | Q77, Q78 | Note audience model already supports it. |
| Allergy prominence | Q76 | Flag severity already supports it. |
| Devices and screen constraints | Q96–Q98 | Sets responsive floor. |
| Part 3 ideas worth building | Part 3 | Every one is a query over the model in `PLAN.md` §2; none needs new capture. |
| What takes way longer than it should | Part 2 | The highest-value answer in the document. Re-read it before every phase. |
