# Live FileMaker exports: student/family PII, never committed.
# Profiling output (aggregates only) goes to docs/discovery/ instead.
resources/exports/
*.mer

# Local agent config
.claude/

# Institutional billing artifacts: invoices, portal screenshots, contracts (PII).
resources/artifacts/

# Raw live export (2026-09-13/14) and the unredacted Q&A originals: student/family PII
# and embedded screenshots. Sanitised copies go to docs/; the raw folders never commit.
resources/application-data/
resources/q-and-a-context/
